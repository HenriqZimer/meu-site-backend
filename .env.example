# ============================================================
# DATABASE CONFIGURATION
# ============================================================
# MongoDB connection string
# Format: mongodb://username:password@host:port/database?authSource=admin
MONGODB_URI=mongodb://localhost:27017/portfolio

# ============================================================
# SERVER CONFIGURATION
# ============================================================
# Port onde a API será executada
BACKEND_PORT=5000

# Ambiente de execução (development, production, test)
NODE_ENV=development

# ============================================================
# SECURITY & AUTHENTICATION
# ============================================================
# Secret key para assinar tokens JWT
# IMPORTANTE: Use uma string aleatória forte em produção (mínimo 32 caracteres)
# Você pode gerar uma com: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Credenciais do usuário admin padrão (criado automaticamente no primeiro boot)
# IMPORTANTE: Altere estas credenciais após o primeiro login em produção!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-strong-password-123

# ============================================================
# CORS CONFIGURATION
# ============================================================
# Origens permitidas para CORS (separadas por vírgula)
# Use '*' para aceitar todas (apenas em desenvolvimento)
# Exemplo de produção: CORS_ORIGIN=https://meusite.com,https://www.meusite.com
CORS_ORIGIN=*

# ============================================================
# SMTP EMAIL CONFIGURATION
# ============================================================
# Configuração do servidor SMTP para envio de emails
# Necessário para notificações do formulário de contato

# Host do servidor SMTP
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
# SendGrid: smtp.sendgrid.net
SMTP_HOST=smtp.gmail.com

# Porta do servidor SMTP
# 587 - TLS (recomendado)
# 465 - SSL
SMTP_PORT=587

# Usar conexão segura SSL/TLS
# false para porta 587 (STARTTLS)
# true para porta 465 (SSL)
SMTP_SECURE=false

# Usuário para autenticação SMTP
# Para Gmail, use uma senha de aplicativo (app password):
# https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com

# Senha para autenticação SMTP
# IMPORTANTE: Use senhas de aplicativo, não sua senha principal!
SMTP_PASS=your-app-password

# Email do remetente (exibido no campo "De:")
SMTP_FROM=noreply@henriqzimer.com.br

# Email do administrador (receberá notificações de contato)
ADMIN_EMAIL=admin@henriqzimer.com.br

# ============================================================
# RATE LIMITING (opcional - para implementação futura)
# ============================================================
# Número máximo de requisições por janela de tempo
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100
